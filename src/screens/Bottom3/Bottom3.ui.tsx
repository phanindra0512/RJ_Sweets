import { View, Text } from "react-native";
import React from "react";
import styles from "./Bottom3.styles.ts";

const Bottom3 = ({ navigation }) => {
  return (
    <View style={{ flex: 1 }}>
      <Text>Bottom3</Text>
    </View>
  );
};

export default Bottom3;
